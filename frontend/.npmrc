# Enable side-effects cache (caches compiled native binaries like canvas)
side-effects-cache=true

# Prefer offline packages from cache when available
prefer-offline=true

# Use frozen lockfile in CI/production to ensure reproducible builds
# frozen-lockfile=true
